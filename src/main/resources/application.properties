spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:3306/${DB_NAME:booking}
spring.datasource.username=${USER_NAME:root}
spring.datasource.password=${USER_PASSWORD:123456}
spring.jpa.hibernate.ddl-auto=update
server.port=8081

#
#keycloak.realm = movie
#keycloak.auth-server-url = http://localhost:8080
#keycloak.resource = booking-webapp
#keycloak.public-client = true
##keycloak.use-resource-role-mappings = true
##keycloak.bearer-only = true
#
#keycloak.securityConstraints[0].authRoles[0] = user
#keycloak.securityConstraints[0].securityCollections[0].patterns[0] = /user/*


